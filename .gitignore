# Especifico da maquina
local.properties

# Saidas de build
.gradle/
build/
app/build/
*.apk
*.aab
*.idsig

# Chave de assinatura: nunca versionar. Quem a tiver publica atualizacao em
# nome do app e instala por cima, no aparelho de quem ja tem.
*.keystore
*.jks

# Onde a chave esta e qual a senha dela. O `.exemplo` ao lado, esse sim, e
# versionado: diz o que preencher sem guardar o segredo.
keystore.properties

# Os .apk prontos para mandar. Sai um por versao, e nenhum deles e fonte.
dist/

# IDE
.idea/
*.iml
.kotlin/
